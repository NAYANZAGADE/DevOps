apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    alb.ingress.kubernetes.io/actions.root-service: |
      {
        "Type": "forward",
        "ForwardConfig": {
          "TargetGroups": [
            {
              "Weight": 50,
              "ServiceName": "flask-failure-rollout-stable",
              "ServicePort": "8000"
            },
            {
              "Weight": 50,
              "ServiceName": "flask-failure-rollout-canary",
              "ServicePort": "8000"
            }
          ]
        }
      }
    alb.ingress.kubernetes.io/backend-protocol: HTTP
    alb.ingress.kubernetes.io/certificate-arn: arn:aws:acm:ap-south-1:555108715761:certificate/4cd34ed2-bb91-43b1-9cd3-d8dc4671274a
    alb.ingress.kubernetes.io/group.name: test
    alb.ingress.kubernetes.io/group.order: '2'
    alb.ingress.kubernetes.io/healthcheck-interval-seconds: '300'
    alb.ingress.kubernetes.io/healthcheck-path: /metrics
    alb.ingress.kubernetes.io/healthy-threshold-count: '2'
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTPS": 443},{"HTTP": 80},{"HTTP": 8000}]'
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/ssl-redirect: '443'
    alb.ingress.kubernetes.io/success-codes: 200-499
    alb.ingress.kubernetes.io/target-type: ip
    alb.ingress.kubernetes.io/unhealthy-threshold-count: '5'
    alb.ingress.kubernetes.io/subnets: subnet-07de86a36d8057b75,subnet-06eb5cbd644f8b68d    
  labels:
    app: flask-failure
  name: app-service
spec:
  ingressClassName: alb
  rules:
    - host: rollouts.nayanzagade.xyz
      http:
        paths:
          - backend:
              service:
                name: root-service
                port:
                  name: use-annotation
            path: /metrics
            pathType: Prefix
          - backend:
              service:
                name: root-service
                port:
                  name: use-annotation
            path: /failure
            pathType: Prefix